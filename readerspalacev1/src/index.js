import React from 'react';
import ReactDOM from 'react-dom/client';
import App from '/Users/jazzyme/Documents/GitHub/readersPalace/readerspalacev1/src/App.js'; // Ensure App.js is correctly set up to export a React component

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);